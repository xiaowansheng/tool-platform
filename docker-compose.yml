services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: "production"
      NEXT_TELEMETRY_DISABLED: "1"
    ports:
      - "${TOOL_PLATFORM_PORT:-3000}:3000"
    volumes:
      - tool_platform_data:/app/data

volumes:
  tool_platform_data:
