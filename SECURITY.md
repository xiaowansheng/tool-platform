# Security Policy

Tool Platform 包含安全、隐私、AI、Sandbox、文件处理和浏览器能力相关工具。请不要在公开 issue 中披露可被利用的漏洞细节。

## 报告漏洞

请通过私密渠道报告安全问题：

- 如果仓库启用了 GitHub Security Advisories，请优先使用 Private vulnerability reporting。
- 如果尚未启用，请联系项目维护者，并在标题中包含 [security] Tool Platform。

报告时请尽量包含：

- 受影响的工具、包或页面路径。
- 复现步骤和最小输入样例。
- 影响范围，例如 XSS、任意脚本执行、数据泄露、供应链风险、权限绕过等。
- 你已经验证过的版本、浏览器和操作系统。

## 安全范围

优先处理：

- XSS、HTML/脚本注入、Sandbox 逃逸。
- 不安全的文件解析、下载或对象 URL 处理。
- 远程请求导致的敏感数据泄露。
- 密钥、token、证书、PII 处理不当。
- 依赖供应链、构建脚本或生成 registry 的安全问题。
- AI 工具中可能导致用户数据外传或提示词泄露的问题。

通常不作为漏洞处理：

- 只影响本地开发环境且无法影响用户数据的问题。
- 没有安全影响的 UI 展示错误。
- 缺少速率限制但项目当前没有公开写入 API 的场景。

## 数据处理原则

- 默认优先本地处理用户输入。
- 如工具需要网络请求或远程 AI/API，应在工具 UI 或文档中说明。
- 不应提交真实密钥、生产日志、用户文件或可识别个人信息作为测试样例。
- 示例数据应使用合成数据，并避免真实域名、真实 token 或真实身份信息。

## 维护者响应

维护者会尽力：

- 确认报告并评估影响。
- 在修复可用前避免公开利用细节。
- 在修复后通过 changelog、release note 或安全公告说明影响范围。
